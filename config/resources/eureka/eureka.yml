server:
  port: 8761
spring:
  application:
    name: eureka
eureka:
  client:
    register-with-eureka: false   #防止自注册
    fetch-registry: false         #不在本地缓存注册表信息 注册中心为false，客户端需要改为true
#  server:
#    waitTimeInMsWhenSyncEmpty: 5  #在服务器接收请求之前等待的初始时间